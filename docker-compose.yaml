services:
  order-service:
    build: ./demo_services/order_service
    ports:
      - '8001:8001'
    environment:
      - SERVICE_NAME=order-service
      - 'SPLUNK_HEC_URL=http://host.docker.internal:8088'
      - 'SPLUNK_HEC_TOKEN=${SPLUNK_HEC_TOKEN}'
      - SPLUNK_INDEX=thread_logs
      - 'PAYMENT_SERVICE_URL=http://payment-service:8002'
  payment-service:
    build: ./demo_services/payment_service
    ports:
      - '8002:8002'
    environment:
      - SERVICE_NAME=payment-service
      - 'SPLUNK_HEC_URL=http://host.docker.internal:8088'
      - 'SPLUNK_HEC_TOKEN=${SPLUNK_HEC_TOKEN}'
      - SPLUNK_INDEX=thread_logs
      - 'INVENTORY_SERVICE_URL=http://inventory-service:8003'
      - 'SIMULATE_FAILURE=${SIMULATE_FAILURE:-false}'
  inventory-service:
    build: ./demo_services/inventory_service
    ports:
      - '8003:8003'
    environment:
      - SERVICE_NAME=inventory-service
      - 'SPLUNK_HEC_URL=http://host.docker.internal:8088'
      - 'SPLUNK_HEC_TOKEN=${SPLUNK_HEC_TOKEN}'
      - SPLUNK_INDEX=thread_logs
  thread-platform:
    build: ./thread_platform
    ports:
      - '9000:9000'
    environment:
      - 'SPLUNK_HEC_URL=http://host.docker.internal:8088'
      - SPLUNK_INDEX=thread_logs
      - 'SPLUNK_MCP_URL=http://host.docker.internal:7700'
      - 'SPLUNK_BASE_URL=http://host.docker.internal:8000'
      - SPLUNK_USERNAME=admin
      - SLACK_SOCKET_MODE=true
      - 'SLACK_ALERT_CHANNEL=#thread-alerts'
      - 'ORDER_SERVICE_URL=http://order-service:8001'
      - PLATFORM_PORT=9000
      - PLATFORM_ENV=development
      - 'SPLUNK_HEC_TOKEN=${SPLUNK_HEC_TOKEN}'
      - 'SPLUNK_MCP_TOKEN=${SPLUNK_MCP_TOKEN}'
      - 'SPLUNK_PASSWORD=${SPLUNK_PASSWORD}'
      - 'SLACK_BOT_TOKEN=${SLACK_BOT_TOKEN}'
      - 'SLACK_SIGNING_SECRET=${SLACK_SIGNING_SECRET}'
      - 'SLACK_APP_TOKEN=${SLACK_APP_TOKEN}'
    depends_on:
      - order-service
